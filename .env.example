# Optional: enables the Hugging Face Inference Providers adapter.
# Create a fine-grained token with “Make calls to Inference Providers” permission.
HF_TOKEN=
# Model IDs and routing policies are configurable. The mock provider remains the default.
HF_MODEL=openai/gpt-oss-120b:cheapest
